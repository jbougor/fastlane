This s is the README file for the FASTLANE set of functionality 
for the Big Data Ingestion  Project.

This piece has 2 distinct pieces:

A push function that takes a local set of directories and sends 
them to a remote machine.  This task was beroken down in to 
pieces to be able to be run in a parallel setup.
